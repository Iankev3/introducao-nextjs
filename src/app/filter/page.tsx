export function Filter() {
    const Frutas = ['maçã', 'uva', 'malencia', 'carambola']

    const resultado = Frutas.filter((fruta)=>{
        const primeiraLetra = fruta[0]

    })

    return (
        <div>

        </div>
    )
}